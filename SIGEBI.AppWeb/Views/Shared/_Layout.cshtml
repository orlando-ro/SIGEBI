<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SIGEBI</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SIGEBI.AppWeb.styles.css" asp-append-version="true" />

    <script>
        (function() {
            const savedTheme = localStorage.getItem('sigebi-theme') || 'light';
            document.documentElement.setAttribute('data-theme', savedTheme);
        })();
    </script>
</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-lg shadow-sm mb-4" style="background-color: var(--theme-primary, #f8f9fa); transition: background-color 0.3s ease;">
            <div class="container">
                <a class="navbar-brand fw-bold" asp-area="" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-book-half text-primary"></i> SIGEBI
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link fw-semibold" asp-area="" asp-controller="Home" asp-action="Index">
                                <i class="bi bi-house-door"></i> Inicio
                            </a>
                        </li>

                        @if (User.Identity!.IsAuthenticated)
                        {
                            var rol = User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value;
                            bool esPersonalAutorizado = rol == "PersonalBibliotecario" || rol == "Administrador" || rol == "Auditor";
                            bool esAdminOAuditor = rol == "Administrador" || rol == "Auditor";

                            // Validaciones estrictas por Módulo
                            bool puedeVerSolicitudes = rol != "Auditor";
                            bool puedeVerPenalizaciones = rol != "Auditor";

                            <li class="nav-item">
                                <a class="nav-link fw-semibold text-primary" asp-area="" asp-controller="Catalogo" asp-action="Index">
                                    <i class="bi bi-collection-fill"></i> Catálogo
                                </a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link fw-semibold" asp-area="" asp-controller="Prestamos" asp-action="Index">
                                    <i class="bi bi-journal-bookmark"></i> Préstamos
                                </a>
                            </li>

                            @if (puedeVerSolicitudes)
                            {
                                <li class="nav-item">
                                    <a class="nav-link fw-semibold" asp-area="" asp-controller="Solicitudes" asp-action="Index">
                                        <i class="bi bi-inbox"></i> Solicitudes
                                    </a>
                                </li>
                            }

                            @if (esPersonalAutorizado)
                            {
                                <li class="nav-item">
                                    <a class="nav-link fw-semibold" asp-area="" asp-controller="Devoluciones" asp-action="Index">
                                        <i class="bi bi-box-arrow-in-left"></i> Devoluciones
                                    </a>
                                </li>
                            }

                            @if (puedeVerPenalizaciones)
                            {
                                <li class="nav-item">
                                    <a class="nav-link fw-semibold text-danger" asp-area="" asp-controller="Penalizaciones" asp-action="Index">
                                        <i class="bi bi-exclamation-triangle-fill"></i> Penalizaciones
                                    </a>
                                </li>
                            }

                            @if (esPersonalAutorizado)
                            {
                                <li class="nav-item">
                                    <a class="nav-link fw-semibold" asp-area="" asp-controller="Reportes" asp-action="Index">
                                        <i class="bi bi-bar-chart-fill"></i> Reportes
                                    </a>
                                </li>
                            }

                            @if (esAdminOAuditor)
                            {
                                <li class="nav-item">
                                    <a class="nav-link fw-semibold text-info" asp-area="" asp-controller="Notificaciones" asp-action="HistorialGlobal">
                                        <i class="bi bi-envelope-paper-fill"></i> Log Notificaciones
                                    </a>
                                </li>

                                <li class="nav-item">
                                    <a class="nav-link fw-semibold text-secondary" asp-area="" asp-controller="Auditoria" asp-action="Index">
                                        <i class="bi bi-shield-lock-fill"></i> Auditoría
                                    </a>
                                </li>
                            }
                        }
                    </ul>

                    <ul class="navbar-nav align-items-center">

                        @if (User.Identity.IsAuthenticated)
                        {
                            <!-- CAMPANITA DE NOTIFICACIONES CORREGIDA (Visible y contrastada en cualquier tema) -->
                            <li class="nav-item me-3">
                                <a class="btn btn-outline-warning border-0 rounded-circle shadow-sm position-relative d-flex align-items-center justify-content-center notification-bell" style="width: 40px; height: 40px;" asp-controller="Notificaciones" asp-action="Index" title="Mis Notificaciones">
                                    <i class="bi bi-bell-fill fs-5"></i>
                                </a>
                            </li>
                        }

                        <li class="nav-item me-4">
                            <div class="btn-group shadow-sm" role="group" aria-label="Selector de temas">
                                <button type="button" class="btn btn-sm btn-outline-secondary" onclick="setTheme('light')" title="Modo Claro">☀️</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" onclick="setTheme('warm-dark')" title="Nocturno Cálido">🌙</button>
                                <button type="button" class="btn btn-sm btn-outline-secondary" onclick="setTheme('ocean')" title="Azul Institucional">🌊</button>
                            </div>
                        </li>

                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fw-bold" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-person-circle"></i> @User.Identity.Name
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="navbarDropdown">
                                    <li><span class="dropdown-item-text text-muted small">Rol: @User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value</span></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item text-danger" asp-controller="Acceso" asp-action="Salir">
                                            <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="btn btn-primary text-white px-4 rounded-pill shadow-sm" asp-controller="Acceso" asp-action="Login">
                                    <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container flex-grow-1">
        <main role="main" class="pb-5 mb-5">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top text-muted mt-auto py-4" style="background-color: var(--bg-card);">
        <div class="container text-center">
            &copy; 2026 - SIGEBI (Sistema de Gestión de Bibliotecas) - Proyecto ITLA
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script>
        function setTheme(themeName) {
            document.documentElement.setAttribute('data-theme', themeName);
            localStorage.setItem('sigebi-theme', themeName);
        }
    </script>

    <style>
        /* Corrección de visibilidad de la campanita en temas oscuros y azul */
        .notification-bell {
            background-color: rgba(255, 193, 7, 0.15);
            color: #ffc107 !important;
            transition: all 0.2s ease-in-out;
        }

            .notification-bell:hover {
                background-color: #ffc107 !important;
                color: #212529 !important;
                transform: scale(1.05);
            }
    </style>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>